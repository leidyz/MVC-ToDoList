
<!DOCTYPE html>
<html>
<head>
<title>To-Do-List</title>
    <style>
        /* Estilos CSS para filas editables */
        .editable-row {
            background-color: yellow;
        }
    </style>
    <!-- Agrega los estilos de Tailwind CSS -->
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body>
<h1>Create Task</h1>

<form method="POST" action="createTask">
    <label for="task_name">Task Name:</label>
    <input type="text" name="task_name" id="task_name" required>

    <label for="task_description">Task Description:</label>
    <input type="text" name="task_description" id="task_description" required>

    <label for="start_date">Start Date:</label>
    <input type="date" name="start_date" id="start_date" required>

    <label for="finish_date">Finish Date:</label>
    <input type="date" name="finish_date" id="finish_date" required>

    <label for="status">Status:</label>
    <select name="status" id="status" >
        <option value="Not Started">Not Started</option>
        <option value="In Progress">In Progress</option>
        <option value="Completed">Completed</option>
        <option value="Cancelled">Cancelled</option>
    </select>

    <label for="created_by">Created By:</label>
    <input type="text" name="created_by" id="created_by" required>
    
    <button type="submit">Create Task</button>
</form>

    <h1>Task List</h1>
    <?php
 
     ?>
    <?php  if (isset($_SESSION['success_message'])): ?>
        <div class="success-message">
            <?php echo $_SESSION['success_message']; ?>
        </div>
    <?php endif; ?>
   
    <h2>All Tasks </h2>
       
    <table >
        <thead>
            <tr>
                <th>Task Name</th>
                <th>Task Description</th>
                <th>Start Date</th>
                <th>Finish Date</th>
                <th>Status</th>
                <th>Created By</th>
            </tr>
        </thead>
        <tbody>
        
        <?php
        
        $taskDetails= $this->taskDetails;
        //var_dump($taskDetails);
        if (isset($taskDetails) && is_array($taskDetails)): 
            foreach ($taskDetails as $task): 
    ?>  
             <tr class="editable-row">
<tr>
    <td>
        <form method="POST" action="saveTask">
        <td> <input type="hidden" name="task_id" value="<?php echo $task['task_id']; ?>"></td>
        <td><input type="text" name="task_name" value="<?php echo $task['task_name']; ?>" disabled></td>
    
    <td><input type="text" name="task_description" value="<?php echo $task['task_description']; ?>" disabled></td>
    <td><input type="date" name="start_date" value="<?php echo $task['start_date']; ?>" disabled></td>
    <td><input type="date" name="finish_date" value="<?php echo $task['finish_date']; ?>" disabled></td>
    <td><input type="text" name="status" value="<?php echo $task['status']; ?>" disabled></td>
    <td><input type="text" name="created_by" value="<?php echo $task['created_by']; ?>" disabled></td>
    <td>
        <button class="edit-button bg-blue-500 text-white px-2 py-1 rounded">Editar</button>
        <button type="submit" class="save-button bg-green-500 text-white px-2 py-1 rounded" style="display: none;">Guardar</button>
    </td>
</form>
</tr>
                        <td>
                            <form method="POST" action="deleteTask">
                                <input type="hidden" name="task_id" value="<?php echo $task['task_id']; ?>">
                                <button type="submit">Delete</button>
                            </form>
                        </td>
                    </tr>
                <?php endforeach;
            endif; ?>
        </tbody>
    </table>

    <script>
    const editButtons = document.querySelectorAll('.edit-button');
    const saveButtons = document.querySelectorAll('.save-button');

    for (let i = 0; i < editButtons.length; i++) {
        editButtons[i].addEventListener('click', (event) => {
            event.preventDefault();
            const row = editButtons[i].closest('tr');
            row.classList.add('editable-row');
            for (const field of row.querySelectorAll('input')) {
                field.removeAttribute('disabled');
            }
            editButtons[i].style.display = 'none';
            saveButtons[i].style.display = 'block';
        });

        saveButtons[i].addEventListener('click', (event) => {
            event.preventDefault();
            const row = saveButtons[i].closest('tr');
            for (const field of row.querySelectorAll('input')) {
                field.setAttribute('disabled', 'true');
            }
            row.classList.remove('editable-row');
            editButtons[i].style.display = 'block';
            saveButtons[i].style.display = 'none';
            // Envía los datos actualizados al servidor (debes implementar esto)
        });
    }
</script>
            
        </tbody>
    </table>
   
</body>
</html>

